{
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": "../../tsconfig.json",

	"compilerOptions": {
		"types": ["eslint", "node"],
		"outDir": "dist/"
	},
	"include": ["source/**/*"],
	"exclude": ["node_modules/"]
}
